## Visual testing

Start by running a copy of the app with `yarn start`

Then open a second terminal window and run the tests with `yarn test:visual`

The tests will generate and image and you can see the difference in the snapshots folder.

Happy testing!